# UPDATE workflow — ABCDE 改码 manual（原 reinit）

## 1. 对象类型适配
默认对象类型: 沿用上轮 init 判定（读 abcde-manual.md 头注释 framework.mutability + workflow 交叉验证）
改码路径梯度: cfg 改值 → 注册制扩展（@register_xxx）→ 改源码 → 不可改（E 档）
路径上限: 沿用上轮对象类型对应上限
场景注记: 打开已有经验项目，核心是「在所有已有信息中找下一个最值得做的事」。A-routine 优先引用历史最优 LR；E 档 UPDATE 重审窗口可调整（UPDATE workflow 允许改 contract/，PROTOCOL 级约定放行；原 reinit）。

## 2. 5×3 改码参考表
<!-- OVERLAY:5x3 -->
| E | **改什么**: 题面（METRIC_KEYS 等评估键）UPDATE 重审<br>**路径**: UPDATE workflow 可改（PROTOCOL 级约定放行）<br>**踩坑**: 场景号在 cfg + F1 清单，不在 contract；改前确认 EXPERIENCE 继承不破坏 | （同 routine，E 档不升深度） | （同 routine，E 档不升深度） |

## 3. 场景陷阱
- 重复已穷尽的路（未读 EXPERIENCE Tier 矩阵）
- E 档固化（UPDATE 重审窗口可调，非永不可改）
- scope 漂移、过度清理历史轮次
